{
  "name": "met0-praisal",
  "version": "0.1.0",
  "description": "EVE Online Jita 4-4 item appraiser — Cloudflare Pages + Supabase",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev dist --compatibility-date=2024-01-01",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "vite build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "@vitejs/plugin-react": "^4.3.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^5.3.4",
    "wrangler": "^3.60.3"
  }
}
